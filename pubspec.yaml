name: cryptoinvesting_com
description: Real Crypto Investing App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  video_player: ^2.8.2
  google_mobile_ads: ^5.0.0

  # In-app purchases (PRO subscription)
  in_app_purchase: ^3.2.0

  # Sharing
  share_plus: ^10.0.2

  # State management
  provider: ^6.1.2

  # Networking
  http: ^1.2.0
  web_socket_channel: ^2.4.0

  # Charts
  syncfusion_flutter_charts: ^26.1.41

  # Utilities
  intl: ^0.19.0

flutter:
  uses-material-design: true
